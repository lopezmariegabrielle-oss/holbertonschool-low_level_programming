#ifndef VARIADIC_FUNCTIONS_H
#define VARIADIC_FUNCTIONS_H

#include <stdarg.h>

/**
 * struct op - Struct op
 *
 * @type: The operator
 * @f: The function associated
 */
/* Tu pourras ajouter des structures ici plus tard si nécessaire */

int _putchar(char c);
int sum_them_all(const unsigned int n, ...);

#endif
