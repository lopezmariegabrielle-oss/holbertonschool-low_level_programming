#ifndef MAIN_H
#define MAIN_H

/**
 * Prototypes des fonctions de la bibliothèque standard
 * (On peut les inclure ici ou dans les fichiers .c, 
 * mais les prototypes de tes fonctions vont ici)
 */

int _putchar(char c);
char *create_array(unsigned int size, char c);



#endif
